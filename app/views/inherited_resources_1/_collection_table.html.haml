%table{:class => "#{collection.first.class.to_s.downcase.pluralize} collection"}
  %thead
    %tr
      - collection.first.attributes.each_pair do |field, value|
        - next if ['id', 'created_at', 'updated_at'].include?(field)
        %th= field.titleize
  %tbody
    - collection.each do |r|
      %tr
        - r.attributes.each_pair do |field, value|
          - next if ['id', 'created_at', 'updated_at'].include?(field)
          %td= value
        %td= link_to t('general.show'), resource_url(r)
        %td= link_to t('general.edit'), edit_resource_url(r)
        %td= link_to t('general.destroy'), resource_url(r), :confirm => t('general.are_you_sure'), :method => :delete

