= simple_form_for @user do |f|
  = f.input :first_name
  = f.input  :last_name
  = f.input :email
  = f.input :role, :collection => User::ROLES
  = f.input :password
  = f.input :password_confirmation
  %p= f.button :submit, :class => "button"